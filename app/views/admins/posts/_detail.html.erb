<div class='row post-detail'>
  <div class="col-xs-5">
    <div class="checklist">
      <!-- イイね -->
      <div class="good">
        <span class="glyphicon glyphicon-heart" style="color: red;"></span>
        <span class="badge"><%= post.goods.count %>　イイね</span>
      </div>
      <%= render 'admins/posts/review', post: post, average_review: average_review %>
    </div>
    <div class="admin-postimage mt-1">
      <%= attachment_image_tag(post, :post_image, size: "360x260", fallback: "no_image.jpg") %>
    </div>
    <div class="form-3 mt-1">
      <div class="faceicon">
        <%= attachment_image_tag(post.user, :profile_image, :fill, 100, 100, class: "faceicon-img", fallback: "no_image2.jpg") %>
        <p class="fontsize-1"><i class="fas fa-user"></i><%= post.user.name %></p>
      </div>
    </div>
  </div>
  <div class="col-xs-7 post-body">
    <h3 class="title fontshadow-1"><%= post.title %></h3><br>
    <h4>
      <span class="genre">
        <%= post.genre.name %>
      </span>
    </h4>
    <div class="col-xs-10 content"><%= post.body %></div>
  </div>
</div>